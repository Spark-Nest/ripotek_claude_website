# Refresh Assets
When invoked: regenerate favicon PNG variants from the SVG source, verify viewBox matches content bounds, and confirm all referenced image filenames match exact case in git (use `git ls-files` to check).
